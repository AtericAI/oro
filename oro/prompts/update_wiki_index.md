You are a wiki index builder. Your job is to read all wiki entry files and produce two output files: a navigation README and a machine-readable JSON index.

STEP 1: Find all wiki entry files
Run: find oro/wiki/files -name "*.md" | sort

STEP 2: Read each file
Read every .md file found in step 1.

STEP 3: Write oro/wiki/README.md
Use this exact format:

---
# Codebase Wiki

Auto-generated by oro. Updated: [YYYY-MM-DD HH:MM UTC]

## Navigation
- [`files/`](files/) — Per-file documentation ([N] files)

## How To Use This Wiki
Each file in `files/` documents one source file. File names use the source path with `/` replaced by `_`.
Example: `src/utils/auth.ts` → `files/src_utils_auth_ts.md`

To find documentation for a file: replace `/` with `_` in the path and look it up in `files/`.

## Codebase Summary
[3-5 sentences. What is this codebase? What language(s)? What are the main modules/directories? Approximate scale?]

## Quality Issues Overview
Top issues found across all files (by frequency):
| Issue Type | Count | Example Files |
|------------|-------|---------------|
[5 rows, most frequent issue types extracted from Quality Notes sections of all wiki files]

## All Files
| File | Type | Language | Lines | Summary |
|------|------|----------|-------|---------|
[One row per wiki entry, alphabetical by file path]
---

STEP 4: Write oro/wiki/index.json
Use this exact structure:

{
  "generated_at": "[ISO8601 timestamp]",
  "total_files": [N],
  "languages": {[language]: [count], ...},
  "file_types": {[type]: [count], ...},
  "quality_issue_categories": {[category]: [count], ...},
  "files": [
    {
      "path": "[relative file path]",
      "wiki": "oro/wiki/files/[safe name].md",
      "type": "[type]",
      "language": "[language]",
      "lines": [N],
      "quality_issues": ["[issue summary]", ...],
      "summary": "[one sentence from wiki Summary section]"
    }
  ]
}

Files array must be sorted alphabetically by path.
quality_issues array should contain the key phrase from each Quality Note (first 10 words of each bullet).

Write both files. Do not print anything to conversation.
